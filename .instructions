# This is the instruction set for the home mango
# In a bootstrapped distribution, this folder will be automatically allocated to /home/<usr>,
# making the user's home directory a mango repo.
# This in turn enables that the built-in mango @<host commands> can be called from anywhere under the user's home directory.

init: init i
del: del d
add: add a
rm: remove rm r
list: list l
edit: edit e
bind: bind b
unbind: unbind ub
